[project]
name = "caption"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pillow>=10.0"]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
